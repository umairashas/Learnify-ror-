<h1>Welcome to Our E-Learning Platform</h1>
<p>Start learning today with our expert-led courses.</p>

<h1 class="text-3xl font-bold underline"></h1>

<% if current_user.role == 'teacher' || current_user.admin? %>
  <% if current_user.role.present? %>
    <%= link_to "Add Course", new_course_path(teacher_id: current_user.id), class: "btn btn-primary" %>
  <% end %>
  <%= link_to "Add Quiz", new_quiz_path, class: "btn btn-primary" %> <br>
  <%= link_to "View All Courses", courses_path, class: "btn btn-primary" %>
  <%= link_to "View All Quizzes", quizzes_path, class: "btn btn-primary" %>
<% end %>

