<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate of Completion</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .certificate-container {
            padding: 40px;
            width: 1024px;
            background: #fff;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
            border: 15px solid #0C5280;
            text-align: center;
            position: relative;
        }
        .certificate-container::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://image.ibb.co/ckrVv7/water_mark_logo.png') no-repeat center;
            background-size: contain;
            opacity: 0.1;
            z-index: -1;
        }
        .certificate-header img {
            width: 100px;
        }
        h1 {
            font-size: 48px;
            color: #0C5280;
            margin-bottom: 10px;
        }
        .student-name {
            font-size: 28px;
            font-weight: bold;
            color: #333;
            margin: 20px 0;
        }
        .certificate-content {
            font-size: 18px;
            color: #555;
            margin-top: 20px;
        }
        .course-title {
            font-size: 24px;
            color: #0C5280;
            font-weight: bold;
            margin-top: 20px;
        }
        .certificate-footer {
            margin-top: 50px;
            font-size: 16px;
            color: #777;
        }
        .signature {
            margin-top: 40px;
        }
        .signature p {
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="certificate-container">
        <div class="certificate-header">
            <img src="images.png" alt="Logo">
        </div>
        <h1>Certificate of Completion</h1>
        <p>This is to certify that</p>
        <p class="student-name"><%= current_user.student.name %></p>
        <p>has successfully completed the course</p>
        <p class="course-title"><%= @course.title %></p>
        <p class="certificate-content">on <%= Date.today.strftime("%B %d, %Y") %></p>
        <div class="signature">
            <p>___________________________</p>
            <p>Authorized Signature</p>
        </div>
        <div class="certificate-footer">
            <p>Accredited by | Endorsed by</p>
        </div>
    </div>
</body>
</html>
